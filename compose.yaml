services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=person_service_db'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5432'
